import { IsFormGroupPipe } from './is-form-group.pipe';

describe('IsFormGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new IsFormGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
