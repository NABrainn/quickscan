import { FileUploadNextStepDirective } from './file-upload-next-step.directive';

describe('FileUploadNextStepDirective', () => {
  it('should create an instance', () => {
    const directive = new FileUploadNextStepDirective();
    expect(directive).toBeTruthy();
  });
});
