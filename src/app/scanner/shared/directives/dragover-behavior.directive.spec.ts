import { DragoverBehaviorDirective } from './dragover-behavior.directive';

describe('DragoverBehaviorDirective', () => {
  it('should create an instance', () => {
    const directive = new DragoverBehaviorDirective();
    expect(directive).toBeTruthy();
  });
});
