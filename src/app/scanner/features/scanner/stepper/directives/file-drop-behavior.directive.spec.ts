import { FileDropBehaviorDirective } from "./file-drop-behavior.directive";

describe('FileUploadNextStepDirective', () => {
  it('should create an instance', () => {
    const directive = new FileDropBehaviorDirective();
    expect(directive).toBeTruthy();
  });
});
