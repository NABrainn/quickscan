import { ObjectDisplayPipe } from './object-display.pipe';

describe('ObjectDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
