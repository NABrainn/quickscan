import { FileSelectBehaviorDirective } from './file-select-behavior.directive';

describe('FileSelectBehaviorDirective', () => {
  it('should create an instance', () => {
    const directive = new FileSelectBehaviorDirective();
    expect(directive).toBeTruthy();
  });
});
