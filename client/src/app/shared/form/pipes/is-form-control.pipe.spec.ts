import { IsFormControlPipe } from './is-form-control.pipe';

describe('IsFormControlPipe', () => {
  it('create an instance', () => {
    const pipe = new IsFormControlPipe();
    expect(pipe).toBeTruthy();
  });
});
